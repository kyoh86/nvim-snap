# nvim-snap 設計メモ

このドキュメントは設計方針やレイヤー構成の議論をまとめるためのものです。
READMEはユーザー向けの使い方に集中させ、設計/実装の話はここに集約します。

## 目的

- NeovimのUI描画をスナップショット化してJSON/ANSI/HTMLで出力する

## 低レイヤーの責務（現状）

- `capture` はシナリオと設定どおりに指定のディレクトリへ出力するだけ
- `normalize` は指定JSONの正規化だけ
- `compare` は指定JSON/ディレクトリ同士の比較だけ
- 「case」などの管理単位は意識しない（すべて明示的なパス指定）

## 高レイヤーの責務（案）

- case単位で「シナリオ / 設定 / 期待値 / 出力物」をまとめて管理する
- 既定パスや命名規則を提供して、低レイヤーの実行を組み合わせる
- 複数シナリオの実行・比較・更新を一括で回せるようにする
- 低レイヤーの成果物（JSON/ANSI/HTML）を利用してレポートを生成する

## 論点

論点はGitHub Issueで管理する。
